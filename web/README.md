# FeeFlow 运营后台

Vue 3 + Vite 的 FeeFlow 企业费控运营工作台，连接同成本中心中的 Go Gin API。页面覆盖经营总览、费用科目、费用报销、付款审批、预算提醒和费用流水。

```bash
npm install
npm test
npm run dev
```

未配置 `VITE_API_BASE_URL` 时自动进入离线演示，演示数据为虚构内容。配置后端地址并登录即可请求 `/api/v1`。


